var assert=required('assert');
var appTest=required('../assets/js/app.js');

describe('reto portafolio test',function(){
   var app;
   before(function(){
     app=new appTest.Portafolio();
   });

   it('porder añadir recurso',function(){
     assert.deepEqual([],);
   });
   it('poder eliminar recurso',function(){
     assert.deepEqual([],);
   });
});
